# URL del bridge WebSocket a cui l'app si connette.
# Se non impostato, l'app usa  wss://<host-della-pagina>/ws/matrix-g2
# Sul device reale punta all'IP/host del bridge (via VPN o dominio pubblico).
VITE_BRIDGE_URL=wss://your-domain.example/ws/matrix-g2
